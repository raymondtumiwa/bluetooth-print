import 'package:flutter/material.dart';

class PickUpCompleted extends StatefulWidget {
  const PickUpCompleted({Key? key}) : super(key: key);

  @override
  _PickUpCompletedState createState() => _PickUpCompletedState();
}

class _PickUpCompletedState extends State<PickUpCompleted> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
